# Agent adapters

Provider adapters will implement network authentication and model execution in the next phase.
The registry in this phase deliberately stores provider-neutral agent records so the shared project context does not depend on a specific AI vendor.
